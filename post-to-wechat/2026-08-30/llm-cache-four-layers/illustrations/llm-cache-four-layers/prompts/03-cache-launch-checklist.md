# 正文图 3：五个静默失效点与 12 项上线检查

生成 1 张 16:9 横版中文生产检查知识图，蒸馏小余知识卡 / Deep Research Sketchnote 风格。

主题：五个精确缓存静默失效点，以及覆盖精确复用与语义安全的 12 项上线检查。

核心信息：缓存失败经常不报错；精确缓存 miss 悄悄增加成本和延迟，Semantic Cache false hit 悄悄损害正确性。

版式：

- 暖奶油纸底，顶部居中标题：`缓存不报错，也可能早已失效`
- 左侧约 32% 是警示容器，标题 `5 个静默失效点`，内部五行：
  1. `前缀塞变量`
  2. `工具顺序变`
  3. `渲染设置变`
  4. `历史被改写`
  5. `模型路由变`
- 中间画一条清楚箭头：`静默失效 → 上线体检`
- 右侧约 68% 是大容器，标题 `上线前 12 查`，分三张卡：
  - 浅蓝卡 `前缀结构`：`稳定内容在前`、`变量内容在后`、`缓存边界固定`、`工具顺序固定`
  - 薄荷绿卡 `运行配置`：`配置开关固定`、`历史尽量只追加`、`模型路由固定`、`核对 Token ID`
  - 浅黄淡粉卡 `语义安全`：`关键实体入 Key`、`TTL 按业务设`、`阈值按流量测`、`答案验后入库`
- 每项前有清楚手绘复选框，行距充足
- 底部全宽结论条：`精确缓存看复用；语义缓存看错答`

严格只有 5 个失效点和 12 个检查项。中文字号要大，430px 手机端缩放后仍能读出分组标题和每项短标签。

只使用以上给定文字，不增加清单项、品牌 Logo、伪代码或随机英文。不要写实、3D、赛博朋克、企业 PPT、暗色背景、复杂渐变和密集小字。
