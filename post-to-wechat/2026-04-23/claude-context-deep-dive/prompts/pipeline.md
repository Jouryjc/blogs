生成一张中文技术信息图，解释 Claude Context 的索引流程。中文文案必须清晰可读。

标题文字：
代码库如何变成 AI 可搜索的上下文？

请画成从左到右的 5 步流程，并准确写入这些标签：
1. 扫描文件
2. 过滤噪音
3. AST 切块
4. 生成 Embedding
5. 写入向量库

每一步下面放一句短说明：
- 只收需要的源码
- 跳过构建产物
- 保留函数和类结构
- 把代码变成向量
- 支持后续语义搜索

视觉元素：
- 源码文件、过滤漏斗、代码 chunk 卡片、向量点阵、数据库圆柱。
- 用箭头连接每一步。
- 画面底部加一句结论：不是塞满上下文，而是按需取相关代码。

排版要求：
- 16:9 横向信息图。
- 白色或浅色背景，文字黑色或深灰，重点用青色和绿色。
- 标签少而大，适合手机阅读。
- 不要伪文字，不要英文乱码，不要 logo。
