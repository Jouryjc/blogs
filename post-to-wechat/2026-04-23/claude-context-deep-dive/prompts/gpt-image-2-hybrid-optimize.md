Optimize this image as a Chinese technical infographic explaining hybrid code search.

Keep these exact readable labels:
- 为什么代码搜索不能只靠向量？
- 语义向量检索
- 找“意思相近”的代码
- BM25 关键词检索
- 找“名字匹配”的代码
- RRF 重排
- 相关代码片段
- 语义负责方向，关键词负责精确，重排负责取舍。

Improve:
- Make the two paths visually clear: semantic vector path and BM25 keyword path.
- Improve Chinese typography and spacing.
- Make result cards clean and readable on mobile.

Do not add extra text, logos, watermarks, or fake UI text.
Use white/light background with cyan, amber, and green accents.
Aspect ratio: 16:9.
