请生成 1 张原生 16:9 横版中文技术知识卡，使用蒸馏小余知识卡 / Deep Research Sketchnote 风格：暖奶油纸底、深海军蓝手绘线、低饱和便签卡片、经典蓝强调、清晰箭头、手机端可读。不要照片、3D、赛博朋克、企业 PPT 或真实品牌 Logo。

主题：Thread、Turn、Item 三种生命周期。

标题：`一次输入，不是一条回复`

构图：用一个大容器包住内部时间线。

大容器标题 `Thread｜可恢复的会话`。

容器内横向放两个浅色分组：`Turn 1`、`Turn 2`。每个 Turn 内有 3 到 5 张小卡，按时间顺序用箭头连接。

`Turn 1` 小卡：`用户消息` → `Agent 消息` → `命令执行` → `完成`

`Turn 2` 小卡：`用户消息` → `文件修改` → `等待审批` → `继续执行` → `完成`

所有小卡上方用一个小括号标 `Item`。`等待审批` 使用浅黄色暂停符号；`继续执行` 使用绿色箭头。

底部结论条：`Item 可追踪，Turn 有边界，Thread 能续上`

只使用指定文字，不增加代码或长句。所有中文逐字正确，时间线在手机端一眼可读。
