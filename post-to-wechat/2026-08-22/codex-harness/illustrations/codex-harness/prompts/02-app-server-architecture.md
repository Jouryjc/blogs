请生成 1 张原生 16:9 横版中文技术架构知识卡，使用蒸馏小余知识卡 / Deep Research Sketchnote 风格：暖奶油纸底、深海军蓝手绘线、低饱和便签卡片、经典蓝强调、清晰箭头、手机端可读。不要照片、3D、赛博朋克、企业 PPT 或真实品牌 Logo。

主题：Codex App Server 如何连接多种客户端与多个 Core Thread。

标题：`一套 Harness，进入多个客户端`

构图：严格从左到右三段式数据流。

左侧竖排三个浅蓝小卡：`CLI`、`IDE`、`App`。三条箭头汇入中间。

中间是最大圆角容器 `App Server`，容器内部从左到右两个模块：

- `消息处理`，小标签 `请求分发 · 事件翻译`
- `Thread Manager`，小标签 `创建 · 恢复 · 分叉`

客户端与 App Server 之间放一条双向粗箭头，箭头上方写 `JSON-RPC`，下方写 `事件 · 审批`。

右侧是两个并列小容器：`Core Thread A`、`Core Thread B`，每个内部只画一个小型 Agent 循环图标。`Thread Manager` 分两条箭头连接它们。

底部结论条：`客户端看稳定协议，Core 专注运行 Agent`

只使用指定文字，不添加多余模块。所有中文逐字正确，箭头方向和层级必须准确。
