请生成 1 张原生 2.35:1 超宽横版中文公众号封面，使用蒸馏小余知识卡 / Deep Research Sketchnote 风格：暖奶油纸底、深海军蓝手绘线、低饱和浅蓝/薄荷绿/浅黄/浅桃便签卡、经典蓝强调。不要先生成方图再补纯色边，不要照片、3D、赛博朋克、企业 PPT 或真实品牌 Logo。

主标题：`Codex 难抄的是 Harness`

副标题：`Core · App Server · 事件与审批`

底部短标签：`Thread · Turn · Item`

构图要求：

- 主标题是最大文字，必须排成两行：第一行只写 `Codex`，第二行只写 `难抄的是 Harness`。
- 画布按 1880×800 理解时，全部标题文字的左右边界必须落在 x=590 到 x=1290 内；不得越出中央 700×700 方形安全区。左右宁可留给装饰，也不要让标题横向铺满。
- 中央画一个小型模型内核，外圈是 `Core` 与 `App Server` 两层手绘圆角框。
- 外圈连接 4 个短标签：`上下文`、`工具`、`审批`、`恢复`。
- 最外侧连接三个客户端小卡：`CLI`、`IDE`、`App`，表现同一 Harness 驱动多种产品。
- 主标题、副标题、模型内核、两层框、4 个标签和底部短标签全部收在居中的 1:1 安全区；把画面从正中裁成 800×800 时，以上文字必须完整可读，任何一个字母都不能被截断。
- 左右两侧用可裁切的 Thread 卡片、箭头、终端小窗和纸张涂鸦填满横幅，主体横向占画面宽度至少 78%，不留大块空白。

只生成指定文字，不要添加口号。所有中文逐字正确，英文技术词拼写正确。
