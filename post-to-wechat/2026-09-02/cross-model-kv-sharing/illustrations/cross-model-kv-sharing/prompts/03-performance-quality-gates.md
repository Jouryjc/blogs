生成 1 张 16:9 横版中文技术知识卡，解释论文里“更快不等于无损，跨模型 KV 迁移必须同时过成本门和质量门”。

视觉风格：蒸馏小余知识卡 / Deep Research Sketchnote；暖奶油纸背景 `#F6F0E3`，深海军蓝手绘线，经典蓝圆角卡，浅绿表示通过，橙色表示质量代价和前提；手机端数字要大且清晰，不要写实、3D、企业 PPT、蓝橙海报或真实 Logo。

标题必须准确写成：`更快，不等于无损`

顶部模型牌：`Llama 70B → Qwen 7B`。其下放一张醒目的条件签：`已有源 KV 后的增量成本`。

中央两张大卡并排：

- 左卡 `目标原生`，大字写 `899 ms` 与 `45.7%`。
- 右卡 `KV 迁移`，大字写 `138 ms` 与 `44.0%`。

两卡之间放两个结论章：浅绿 `速度约 6.5×`，浅橙 `准确率 -1.7 个百分点`。

下方画两道串联闸门：第一道 `成本门`，小字 `翻译总成本 < 目标 Prefill`；第二道 `质量门`，小字 `质量 ≥ 业务阈值`。通过后是浅绿出口 `采用迁移`；旁边灰色 fallback 出口 `原生 Prefill`。

底部 takeaway 必须准确写成：`先过成本门，再过质量门`

只使用以上文字。不得把 138 ms 写成完整请求耗时，不得写“零损失”或“普遍 6.5×”。
