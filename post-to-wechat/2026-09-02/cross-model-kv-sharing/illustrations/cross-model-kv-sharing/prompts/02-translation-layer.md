生成 1 张 16:9 横版中文技术知识卡，解释“跨模型 KV 翻译不是 tensor 改形状，而是表示空间适配”。

视觉风格：蒸馏小余知识卡 / Deep Research Sketchnote；暖奶油纸背景 `#F6F0E3`，深海军蓝手绘线，经典蓝 `#0F4C81`，低饱和浅蓝、浅紫和浅绿便签；中央学习式翻译器用橙色；手机端清晰，不要写实、3D、企业 PPT、蓝橙海报或真实 Logo。

标题必须准确写成：`模型不同，KV 也不同`

左侧是 `源模型空间`，画一组蓝色层堆叠和 KV 小方块；右侧是 `目标模型空间`，画形状、层数和 KV 头明显不同的紫蓝层堆叠。中央放橙色齿轮桥，准确标为 `A→B 学习式翻译`。

源端到翻译桥之间放四张短卡，分别写：`层组织不同`、`KV 头不同`、`维度不同`、`分词器不同`。

翻译桥内部只放两个动作：`保留上下文关系`、`适配目标结构`。输出端标签写 `目标兼容 KV`，下方放两个范围标签：`同家族`、`跨家族`。在目标兼容 KV 旁用小公式表示 `兼容 KV ≠ 原生 KV`。

底部 takeaway 必须准确写成：`它对齐的是表示空间，不是改个张量形状`

只使用以上中文标签。不要出现“无需训练”“万能转换器”“任意模型”或“完全无损”。
