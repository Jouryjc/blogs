生成 1 张 16:9 横版中文技术知识卡，主题是“多模型切换时，跨模型 KV 迁移省掉哪一次 Prefill”。

视觉风格：蒸馏小余知识卡 / Deep Research Sketchnote；暖奶油纸背景 `#F6F0E3`，深海军蓝手绘线，经典蓝圆角卡片，橙色只标重复计算，浅绿色标复用路径；手机端清晰，不要写实、3D、企业 PPT、蓝橙海报或真实 Logo。

标题必须准确写成：`上下文，只算一次`

构图为上下两条横向路径：

上半区标题 `传统路径`。同一张卡片 `共享上下文` 分别进入 `模型 A Prefill` 和 `模型 B 再 Prefill`，两次读取之间用橙红括号标 `重复计算`。

下半区标题 `迁移路径`。严格按顺序画：`共享上下文` → `模型 A Prefill` → `源 KV` → `KV 翻译` → `目标可用 KV` → `模型 B 解码`。在路径上方放醒目条件签：`前提：源 KV 已存在`。在模型 B 前放浅绿标签：`跳过完整 Prefill`。

底部 takeaway 必须准确写成：`迁移的不是文字，而是已算出的上下文状态`

只使用以上中文标签，不增加长段说明。必须保留模型 A 的第一次 Prefill；不要画成零成本，也不要画源 KV 直接插入模型 B。
