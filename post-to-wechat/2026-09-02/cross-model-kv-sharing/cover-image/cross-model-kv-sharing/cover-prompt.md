生成 1 张精确 2.35:1 的中文公众号宽横封面，目标尺寸约 1880×800。必须兼容居中 1:1 方形裁切：全部标题、核心流程、关键标签与 takeaway 都放进画布中央的方形安全区，左右边缘只放可裁掉的淡色纸张、KV 小方块和箭头涂鸦。

视觉风格：蒸馏小余知识卡 / Deep Research Sketchnote；暖奶油纸背景 `#F6F0E3`，深海军蓝手绘描边，经典蓝 `#0F4C81`，低饱和浅蓝与浅绿卡片；橙色只突出中间的 KV 翻译层。信息图式封面，不要纯标题海报，不要写实、3D、暗黑赛博、企业 PPT、高饱和蓝底或真实品牌 Logo。

角标：`5 分钟蒸馏`

主标题分两行，必须准确写成：
`模型切换别再重算长上下文`
`KV Cache 开始跨模型接力`

中央画一条紧凑工程链路：`共享上下文` → `源模型` → `源 KV` → 橙色桥梁 `KV 翻译层` → `目标可用 KV` → `目标模型`。在目标模型下方画一条浅灰、被划掉的路径 `重新 Prefill`，让读者看懂跳过的是目标侧完整重算，而不是第一次 Prefill。

底部 takeaway：`不是搬缓存，是翻译状态`

只使用以上文字。不要出现“万能转换器”“任意模型”“零成本”“完全无损”。中央信息要在方形裁切后仍完整、可读。
