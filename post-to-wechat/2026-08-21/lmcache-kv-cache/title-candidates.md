# 标题候选

1. **Agent 上下文越跑越贵，先把 KV Cache 从推理进程里拆出来**（推荐；痛点 + 动作）
2. **Prompt Cache 只能省前缀：LMCache 把 KV Cache 变成独立服务**（认知反差 + 方案）
3. **Agent 总在重读旧材料：这层缓存决定首字等待时间**（场景 + 收益）
4. **从 Prefix Cache 到 CacheBlend：长上下文推理的复用边界**（技术路径）
5. **你的 GPU 可能没在思考，只是在重复做 Prefill**（反常识判断）

最终采用第 1 个。它先说读者遇到的成本问题，再点出文章最重要的架构变化；没有承诺脱离测试条件的提速倍数。

