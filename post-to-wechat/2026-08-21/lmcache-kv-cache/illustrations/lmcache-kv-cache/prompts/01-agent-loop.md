请生成 1 张原生 16:9 横版中文技术知识卡，遵循同目录上三级 `gen-image.md` 的统一视觉系统。

主题：Agent 多轮调用中的重复 Prefill。

标题：`每轮只多一点，却要重读一大段`

构图：左右对照，中间用深蓝竖线分隔。

左侧标题 `没有复用`。画 3 次连续的 Agent 请求，每次都是一列上下文积木：大块 `系统提示词`、`工具定义`、`历史消息`，最底部只有一小块 `新增结果`。三列上方分别标 `第 1 轮`、`第 10 轮`、`第 20 轮`。用回转箭头表现整列反复进入 `Prefill`。

右侧标题 `命中缓存`。画一叠带对勾的深蓝缓存块，标签 `旧 KV`；只有一小块浅黄 `新增结果` 进入 `Prefill`，随后接到 `生成`。

底部结论条：`省下重复输入的计算，不等于输出免费`

不要画价格数字，不要真实品牌 Logo。指定中文必须逐字正确，卡片标签在手机缩略图中可读。

