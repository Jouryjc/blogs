请生成 1 张原生 16:9 横版中文技术知识卡，遵循同目录上三级 `gen-image.md` 的统一视觉系统。

主题：Prompt Cache 能命中什么，在哪里失效。

标题：`Prompt Cache 认的是稳定前缀`

构图：上方一条绿色成功路径，下方两张浅粉边界卡。

上方标签 `可以命中`。从左到右画连续积木：`工具` → `系统` → `历史` → `新增消息`。前三块用同一种浅蓝色并加括号 `稳定前缀`，最后一块用浅黄。绿色对勾只覆盖前三块，表达尾部继续增长仍可复用。

左下卡片标题 `前缀变化`：画 `工具 A · 工具 B` 交换顺序，配一个红色断开的缓存箭头。

右下卡片标题 `文档重组`：第一行 `A · B · C`，第二行 `C · A · B`，配一个红色断开的缓存箭头。

底部结论条：`尾部能增长，前面别乱动`

只使用指定文字，不增加说明段落，不要密集小字。所有中文逐字正确，箭头方向清楚。

