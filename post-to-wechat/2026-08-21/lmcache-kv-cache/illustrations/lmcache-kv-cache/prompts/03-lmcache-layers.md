请生成 1 张原生 16:9 横版中文技术知识卡，遵循同目录上三级 `gen-image.md` 的统一视觉系统。

主题：LMCache 把 KV Cache 变成独立、分层、可回退的服务。

标题：`把 KV Cache 从推理进程里拆出来`

构图：从左到右的数据流，中央独立服务最醒目。

左侧放 3 张浅蓝小卡，分别写 `推理进程 1`、`推理进程 2`、`推理进程 3`，三条箭头都连接中央大卡 `LMCache`。

中央 `LMCache` 卡内只放两个动作标签：`查询`、`异步写入`。

右侧竖向三层缓存架：

- 顶层浅绿：`HBM｜快 · 小`
- 中层浅黄：`CPU｜中 · 大`
- 底层浅桃：`SSD｜慢 · 更大`

中央到三层之间画双向箭头。中央下方另画一条虚线路径绕到左侧 `正常 Prefill`，旁边只写 `未命中 / 超时`，表示缓存失败时回退计算。

底部结论条：`先取旧计算，取不到就照常算`

不要加入远端云、数据库或额外层级。所有中文逐字正确，缓存层级与回退路径在手机端一眼可见。

