请生成 1 张 16:9 横版中文技术知识卡，遵循同目录上三级 `gen-image.md` 的统一视觉系统。

主题：DeepSeek Harness 的 Cordis 插件架构。

标题：`一切皆插件，连 Agent Loop 也能换`

构图：中心是一张奶油白圆角核心卡，写 `Cordis Context`。中心周围以清楚的环形拓扑放置 8 张不同浅色插件卡，每张卡只写一个标签：

- `Agent Loop`
- `LLM`
- `工具`
- `会话`
- `Shell`
- `文件系统`
- `子 Agent`
- `Web UI`

每张卡通过深蓝插槽线连接中心。上方加一条简短装配箭头：`Profile + Bundle + Patch → 运行中的 Agent`。

画面左下用一个小插头表现 `按依赖装上`；右下用一个拔出的插头和收回的虚线表现 `卸载时撤销注册`。不要画成所有插件共享大脑；Context 是服务容器和连接中心。

底部结论条：`不仅要插得进去，还要拔得干净`

确保所有指定中文逐字正确，箭头关系清楚，手机缩略图可读，不增加英文小字。

