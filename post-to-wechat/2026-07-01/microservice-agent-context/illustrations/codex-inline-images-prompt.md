请使用 Codex 的原生图片生成能力，生成 4 张独立的 16:9 横版中文技术知识卡图片，保存为 raster 图片文件。

统一风格：
- 蒸馏小余知识卡 / 技术 sketchnote 风格。
- 奶油色纸张背景，轻微纸纹。
- 深海军蓝手绘线条，低饱和蓝、绿、橙、粉色便签色块。
- 信息密度适合微信公众号正文阅读，不要做成 PPT 模板。
- 不使用真实品牌 Logo，不使用照片，不使用暗色背景，不使用大面积渐变。
- 画面中文字尽量短，中文准确、清晰、可读；英文技术词可以保留。
- 每张图左上角可以有小标签“蒸馏小余知识卡”。
- 生成 4 个独立文件，不要拼成一张长图。

图片 1：workspace-is-not-context
主题：workspace 只是入口，不是上下文。
画面：左侧是一堆微服务文件夹放在同一个 workspace 中；中间是 Agent 拿放大镜找路；右侧分成两个结果卡片：“读太少：漏掉下游契约”“读太多：上下文被稀释”。底部一句短结论：“补服务地图，再让 Agent 动手”。

图片 2：agents-md-routing-map
主题：根 AGENTS.md 做路由，服务 AGENTS.md 写边界。
画面：左侧是“根 AGENTS.md”卡片，内容是“服务清单 / 调用链 / 入口文件”；箭头指向右侧三个服务卡片：“订单服务”“库存服务”“支付服务”。每个服务卡片里只写短标签：“职责边界”“接口契约”“测试命令”。底部一句短结论：“根文件指路，服务文件画边界”。

图片 3：protocol-as-hard-context
主题：手写文档解释意图，机器协议锁住边界。
画面：左侧是“README”纸张，旁边标注“可能过期”；中间是“机器可读协议”大卡片，列出“OpenAPI”“Schema”“Pact”；右侧箭头连接到“mock server”和“contract test”。底部一句短结论：“先读协议，再读说明”。

图片 4：contract-test-loop
主题：契约测试让 Agent 自己跑出证据。
画面：一个环形流程，节点依次是“服务地图”“接口规格”“改代码”“mock + 测试”“失败日志”“自我修正”。中心是友好的 Agent 小机器人。底部一句短结论：“没有测试日志，完成只是形容词”。

输出要求：
- 生成 4 张图片。
- 文件名尽量以 microservice-agent-context 开头。
- 图片比例为 16:9，优先 PNG。
- 保存到调用方指定的输出目录。
