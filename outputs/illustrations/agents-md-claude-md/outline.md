# 配图大纲：AGENTS.md / CLAUDE.md 不是初始化文件

## 图 1：三层记忆结构

目的：解释全局、项目、局部规则如何分层，帮助读者理解不是所有规则都应该塞进一个文件。

画面：
- 顶层：个人习惯，全局文件
- 中层：项目工作方式，仓库根目录文件
- 底层：模块差异，子目录或规则文件
- 右侧提示：越靠近代码，越具体；越全局，越稳定

## 图 2：持续优化闭环

目的：解释真正关键动作不是 `/init`，而是把反复纠正转成短规则，并验证是否改变行为。

画面：
- 发现偏差
- 判断是否重复出现
- 写成一条可执行规则
- 运行小任务验证
- 定期删除过期规则

